const rootDef = require('./root.typedef');
const authDef = require('./auth.typedef');
const carrierDef = require('./carrier.typedef');
const userDef = require('./user.typedef');
const tripDef = require('./trip.typedef');

module.exports = {
	rootDef,
	authDef,
	carrierDef,
	userDef,
	tripDef,
};